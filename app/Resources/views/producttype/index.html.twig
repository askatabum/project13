    {% extends 'baseadmin.html.twig' %}
{% block sectiontitle %}Producttypes{% endblock %}
{% block sectionsubtitle %}wszystkie rekordy{% endblock %}
    {% block body %}
     
        <div class="box">
            <div class="box-header">
                <h3 class="box-title">Producttypes  <small>wszystkie rekordy</small></h3>
                <div class="box-tools pull-right">
                    <a type="button" href="{{ path('producttype_new') }}" class="btn btn-sm btn-success" >
                        <span class="fa fa-plus-square"></span>Utwórz productType
                    </a>
                </div>
            </div>
            <!-- /.box-header -->
            <div class="box-body">
                <div class="table-responsive">
                    <table id="example1" class="table table-bordered table-striped">
                        <thead>
                            <tr>
                                <th>Akcje</th>                                <th>Id</th>                                <th>Name</th>                            </tr>
                        </thead>
                        <tbody>
                            {% for productType in productTypes %}
                            <tr>                                <td>
                                    <a href="{{ path('producttype_show', { 'id': productType.id }) }}">{{ productType.id }}</a>

                                    <a href="{{ path('producttype_show', { 'id': productType.id }) }}">show</a>

                                    <a href="{{ path('producttype_edit', { 'id': productType.id }) }}">edit</a>

                                </td>                                <td>
                                    <a href="{{ path('producttype_show', { 'id': productType.id }) }}">{{ productType.name }}</a>

                                    <a href="{{ path('producttype_show', { 'id': productType.id }) }}">show</a>

                                    <a href="{{ path('producttype_edit', { 'id': productType.id }) }}">edit</a>

                                </td>
                                <td>{{ productType.name }}</td>
                            </tr>
                            {% endfor %}
                        </tbody>
                        <tfoot>
                            <tr>
                                <th>Id</th>
                                <th>Name</th>

                            </tr>
                        </tfoot>
                    </table>
                </div>
            </div>
            <!-- /.box-body -->
        </div>
        <!-- /.box -->   
        <table>
            <thead>
                <tr>

                </tr>
            </thead>
            <tbody>

            </tbody>
        </table>






        {% endblock %}
        